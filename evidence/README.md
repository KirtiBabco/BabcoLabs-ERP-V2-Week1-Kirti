# Evidence

Store concise verification artifacts here.

For each verified build unit record:

- build unit ID;
- what was tested;
- positive result;
- negative/failure result where applicable;
- command/test/API used;
- relevant output, screenshot reference, log, SQL result, or hash;
- commit SHA that was verified.

A build unit is not `[X] Verified Complete` until evidence exists.
