# Week 1 Project 1 - Live Build

Source of truth curriculum: `babcofoods/babco-labs-erp-v2-training-curriculum` (READ ONLY).

Implementation repo: this repository.

Runtime: Node.js 22 + TypeScript + Express 5 + direct `mssql` + Ajv.

API base: `/api/v1/business-requests`

Required environment variable: `AZURE_SQL_CONNECTION_STRING`.

The Azure deployment is isolated in the BABCO R&D sandbox and uses synthetic/training data only.
